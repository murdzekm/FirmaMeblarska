@model IEnumerable<FirmaMeblarska.Models.ZespolPracownik>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<p>
    <a asp-action="CreateZespolPracownik">CreateZespolPracownik</a>
</p>
CreateZespolPracownik
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pracowniks)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Pracowniks.Email)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ZespolId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ZespolId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ZespolId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
