@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Dane konta";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Tutaj możesz pobrać lub usunąć dane z konta.</p>
        <p>
            <strong>Usunięcie tych danych spowoduje doszczętne usunięcie konta i nie bedzie mozna go przywrócić.</strong>
        </p>
        <br />
        <form id="download-data" asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-primary" type="submit">Pobierz</button>
        </form>
        <p>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">Usuń</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}